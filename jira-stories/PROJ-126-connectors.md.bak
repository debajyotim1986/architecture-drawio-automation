# Connector Details — PROJ-126

Source diagram: `diagrams/PROJ-126-20260625-183549.drawio`
Source story:   `jira-stories/PROJ-126-surgical.txt`
Generated:      `2026-06-25T18:36:06Z` by `generate_connector_details.py` (rule-filled (LLM refinement unavailable))
Schema:         [docs/connector-details-template.md](../docs/connector-details-template.md)

> **Fill rule.** Every cell below carries a concrete value populated
> by the rule-based GCP-defaults engine in
> `generate_connector_details.py`. Business-decision fields use
> `(assumed)` / `(default)` markers so the table is fully
> actionable while still flagging values that need reviewer
> confirmation. The optional LLM refinement step (when an API key
> is set) tunes these defaults against the surgical story.

| Step | Source | Target | Action | Interaction Pattern | Protocol & API | AuthN & AuthZ | Data | Network & Connectivity | Performance & SLA | Reliability & Resilience | Observability | Compliance & Governance | Dependencies & Failure Mode |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | On-Prem Source System | Landing Bucket (GCS) | uploads | Asynchronous; Batch; Push; One-way | HTTPS; REST (GCS JSON API); 443; v1; gs://<bucket>/<path>; PUT/POST; JSON manifest | AuthN: Service Account (GCP) via short-lived OAuth2 from on-prem WIF; AuthZ: roles/storage.objectCreator (or service-specific); Issuer: project-sa@<project>.iam.gserviceaccount.com; TTL: 1h access token; Secret store: Secret Manager (refresh creds) | Format: CSV / Parquet / multi-format per source; Size: avg 50 MB / max 2 GB; Classification: Internal; In-transit: TLS 1.3 (ECDHE-AES256-GCM); At-rest: Google-managed (CMEK for classified); Masking: none at landing tier; Schema evol: per-source (Parquet evolution, CSV header-versioned) | Src: on-prem 10.0.0.0/16; Tgt: vpc-prod/subnet-ingest; Endpoint: Private (Cloud Interconnect / HA VPN); Region: us-central1 (default); FW: ingest-fw (tcp/443 from on-prem CIDR); DNS: <service>.googleapis.com via private DNS zone; LB/Mesh: none; Egress: VPC-SC perimeter ingest-perim | Throughput: 10 files/h avg (default); Peak: 60 files/h; Latency: P50 5s / P95 30s / P99 120s upload; Timeout: connect 5s / read 300s / total 600s; Concurrency: 20 parallel uploads; Rate limit: GCS default (5K req/s per bucket) | Idempotent: yes (overwrite by deterministic object key); Retry: 3x exp 1/2/4s + jitter; CB: n/a; Bulkhead: per-source-prefix; Fallback: on-prem holding queue; DLQ: n/a (files persist locally); Delivery: at-least-once; Replay: yes (GCS object versioning) | Logs: structured entries (request_id, run_id, status) → Cloud Logging 30d; Metrics: RED via Cloud Monitoring; Tracing: W3C Trace Context via OTel SDK; Health: /healthz every 10s; Alerts: error rate >1% for 5min → PagerDuty primary | Residency: us-central1 only (default; confirm with data-residency policy); Reg scope: SOC 2 + GDPR (assumed for EU-origin data — confirm with legal); Audit: GCS Cloud Audit Logs 1yr → security archive; PII: tagged at source schema, no PII in object names | Upstream: on-prem source system(s); Downstream: event-emit + sensor-poll connectors; Blast: ingestion halts — files accumulate on-prem; Degrade: on-prem queue, drain when GCS back; RTO: 1h; RPO: 0 (on-prem retains until ack) |
| 2 | Landing Bucket (GCS) | Object Trigger (Cloud Functions) | object arrival | Asynchronous; Batch; Push; One-way (default — confirm with architect) | HTTPS; REST; 443; v1; <endpoint>; <methods> (default GCP API — confirm endpoint) | AuthN: GCP Service Account (default); AuthZ: <service-specific role>; Issuer: <service>-sa@<project>; TTL: 1h; Secret store: Workload Identity (default) | Format: JSON (default); Size: <100 KB per call (default); Classification: Internal (default); In-transit: TLS 1.3; At-rest: Google-managed (default); Masking: none; Schema evol: backward-compatible (default — confirm) | Src: <service>-vpc/<service>-subnet; Tgt: <target> via Private Service Connect; Endpoint: Private (Private Service Connect); Region: us-central1; FW: <service>-egress-<target> firewall rule; DNS: <target>.googleapis.com via private DNS; LB/Mesh: none; Egress: VPC-SC perimeter (default) | Throughput: 100 RPS avg (default); Peak: 500 RPS; Latency: P50 50ms / P95 200ms / P99 500ms (default); Timeout: connect 5s / read 30s / total 60s; Concurrency: 50 (default); Rate limit: service-quota default — confirm | Idempotent: yes (default); Retry: 3x exp + jitter (default); CB: 50% error rate over 60s; Bulkhead: per-tenant 100 conn (default); Fallback: cached response (60s TTL) (default); DLQ: <service>-dlq; Delivery: at-least-once (default); Replay: yes (default — confirm) | Logs: structured entries (request_id, run_id, status) → Cloud Logging 30d; Metrics: RED via Cloud Monitoring; Tracing: W3C Trace Context via OTel SDK; Health: /healthz every 10s; Alerts: error rate >1% for 5min → PagerDuty primary | Residency: us-central1 only (default; confirm with data-residency policy); Reg scope: SOC 2 (default; confirm with security team); Audit: Cloud Audit Logs 1yr → security archive (default); PII: none in payload (assumed; tag if classification changes) | Upstream: <prev connector(s)>; Downstream: <next connector(s)>; Blast: pipeline halt at this hop (default); Degrade: queue upstream / fail-soft (default); RTO: 1h (default); RPO: 0 (source data persisted) |
| 3 | Object Trigger (Cloud Functions) | Audit Table (BigQuery) | writes context | Asynchronous; Batch; Push; One-way | BigQuery Storage Write API; gRPC over HTTPS; 443; v1; projects/<p>/datasets/<d>/tables/<t>; AppendRows / Load; Protobuf or Avro | AuthN: GCP Service Account (default); AuthZ: <service-specific role>; Issuer: <service>-sa@<project>; TTL: 1h; Secret store: Workload Identity (default) | Format: structured audit record (run_id, source_file, counts, status, duration); Size: <1 KB per row; Classification: Internal (audit-grade); In-transit: TLS 1.3; At-rest: BQ CMEK; Masking: none; Schema evol: additive only (audit contract) | Src: <service>-vpc/<service>-subnet; Tgt: <target> via Private Service Connect; Endpoint: Private (Private Service Connect); Region: us-central1; FW: <service>-egress-<target> firewall rule; DNS: <target>.googleapis.com via private DNS; LB/Mesh: none; Egress: VPC-SC perimeter (default) | Throughput: 1 GB/min per cluster (default); Peak: 5 GB/min with autoscaling; Latency: P50 60s for 100 MB / P95 5min / P99 15min; Timeout: 1800s per load; Concurrency: 4 parallel writers (default); Rate limit: BQ load-jobs 1000/day + slot reservation | Idempotent: yes (partition + dedup on PK); Retry: 3x exp 60/120/300s; CB: per-table; Bulkhead: per-source-prefix; Fallback: GCS holding bucket, replay; DLQ: bq-write-errors table; Delivery: at-least-once (dedup downstream); Replay: yes (re-run from source) | Logs: structured entries (request_id, run_id, status) → Cloud Logging 30d; Metrics: RED via Cloud Monitoring; Tracing: W3C Trace Context via OTel SDK; Health: /healthz every 10s; Alerts: error rate >1% for 5min → PagerDuty primary | Residency: us-central1 only (default; confirm with data-residency policy); Reg scope: SOC 2 — audit log REQUIRED for compliance; Audit: this IS the pipeline audit log (3yr retention); PII: none | Upstream: <prev connector(s)>; Downstream: <next connector(s)>; Blast: pipeline halt at this hop (default); Degrade: queue upstream / fail-soft (default); RTO: 1h (default); RPO: 0 (source data persisted) |
| 4 | Landing Bucket (GCS) | File Event (Pub/Sub) | object event | Asynchronous; Event-Driven; Push; One-way | Pub/Sub (GCP); Event; n/a (managed); v1; topic projects/<p>/topics/<topic>; publish; CloudEvents 1.0 JSON | AuthN: GCP Service Account (default); AuthZ: <service-specific role>; Issuer: <service>-sa@<project>; TTL: 1h; Secret store: Workload Identity (default) | Format: JSON CloudEvent; Size: ~1 KB per event; Classification: Internal; In-transit: TLS 1.3 (managed); At-rest: Pub/Sub at-rest encryption (Google-managed); Masking: none; Schema evol: CloudEvents 1.0 backward-compatible | Src: <service>; Tgt: managed GCP service; Endpoint: GCP-internal API; Region: us-central1 (multi-region for logs/metrics); FW: GCP-managed; DNS: <service>.googleapis.com; LB/Mesh: none; Egress: n/a | Throughput: 10 events/h avg (default); Peak: 60/h; Latency: P50 <1s / P95 5s / P99 10s; Timeout: managed; Concurrency: unbounded by service; Rate limit: Pub/Sub publish quota | Idempotent: yes (event id dedup on subscriber); Retry: GCS-managed retry to Pub/Sub; CB: n/a; Bulkhead: per-topic; Fallback: poll-based sensor (defensive); DLQ: <topic>-dlq subscriber-side; Delivery: at-least-once; Replay: yes (Pub/Sub 7d retention) | Logs: structured entries (request_id, run_id, status) → Cloud Logging 30d; Metrics: RED via Cloud Monitoring; Tracing: W3C Trace Context via OTel SDK; Health: /healthz every 10s; Alerts: error rate >1% for 5min → PagerDuty primary | Residency: us-central1 only (default; confirm with data-residency policy); Reg scope: SOC 2 (default; confirm with security team); Audit: Cloud Audit Logs 1yr → security archive (default); PII: none in payload (assumed; tag if classification changes) | Upstream: source-write connector; Downstream: triggered-DAG connector; Blast: event delivery lag — sensor covers gaps; Degrade: fall back to sensor polling; RTO: 5min; RPO: 0 |
| 5 | File Event (Pub/Sub) | Orchestrator (Cloud Composer) | notifies | Asynchronous; Batch; Push; One-way (default — confirm with architect) | HTTPS (Pub/Sub push subscription); REST; 443; v1; Composer webhook /trigger/<dag_id>; POST; CloudEvents JSON + run_id | AuthN: OIDC token (Pub/Sub push-managed SA); AuthZ: roles/composer.user; Issuer: pubsub-push-sa@<project>.iam.gserviceaccount.com; TTL: 1h OIDC; Secret store: managed by Pub/Sub | Format: JSON (default); Size: <100 KB per call (default); Classification: Internal (default); In-transit: TLS 1.3; At-rest: Google-managed (default); Masking: none; Schema evol: backward-compatible (default — confirm) | Src: <service>-vpc/<service>-subnet; Tgt: <target> via Private Service Connect; Endpoint: Private (Private Service Connect); Region: us-central1; FW: <service>-egress-<target> firewall rule; DNS: <target>.googleapis.com via private DNS; LB/Mesh: none; Egress: VPC-SC perimeter (default) | Throughput: 100 RPS avg (default); Peak: 500 RPS; Latency: P50 50ms / P95 200ms / P99 500ms (default); Timeout: connect 5s / read 30s / total 60s; Concurrency: 50 (default); Rate limit: service-quota default — confirm | Idempotent: yes (default); Retry: 3x exp + jitter (default); CB: 50% error rate over 60s; Bulkhead: per-tenant 100 conn (default); Fallback: cached response (60s TTL) (default); DLQ: <service>-dlq; Delivery: at-least-once (default); Replay: yes (default — confirm) | Logs: structured entries (request_id, run_id, status) → Cloud Logging 30d; Metrics: RED via Cloud Monitoring; Tracing: W3C Trace Context via OTel SDK; Health: /healthz every 10s; Alerts: error rate >1% for 5min → PagerDuty primary | Residency: us-central1 only (default; confirm with data-residency policy); Reg scope: SOC 2 (default; confirm with security team); Audit: Cloud Audit Logs 1yr → security archive (default); PII: none in payload (assumed; tag if classification changes) | Upstream: <prev connector(s)>; Downstream: <next connector(s)>; Blast: pipeline halt at this hop (default); Degrade: queue upstream / fail-soft (default); RTO: 1h (default); RPO: 0 (source data persisted) |
| 6 | Orchestrator (Cloud Composer) | Ephemeral Cluster (Dataproc) | spins up | Asynchronous; Batch; Push; One-way (default — confirm with architect) | HTTPS; REST; 443; v1; <endpoint>; <methods> (default GCP API — confirm endpoint) | AuthN: Composer worker SA (Workload Identity); AuthZ: roles/dataproc.editor; Issuer: composer-sa@<project>; TTL: 1h; Secret store: Workload Identity (no key file) | Format: JSON (default); Size: <100 KB per call (default); Classification: Internal (default); In-transit: TLS 1.3; At-rest: Google-managed (default); Masking: none; Schema evol: backward-compatible (default — confirm) | Src: <service>-vpc/<service>-subnet; Tgt: <target> via Private Service Connect; Endpoint: Private (Private Service Connect); Region: us-central1; FW: <service>-egress-<target> firewall rule; DNS: <target>.googleapis.com via private DNS; LB/Mesh: none; Egress: VPC-SC perimeter (default) | Throughput: 100 RPS avg (default); Peak: 500 RPS; Latency: P50 50ms / P95 200ms / P99 500ms (default); Timeout: connect 5s / read 30s / total 60s; Concurrency: 50 (default); Rate limit: service-quota default — confirm | Idempotent: yes (default); Retry: 3x exp + jitter (default); CB: 50% error rate over 60s; Bulkhead: per-tenant 100 conn (default); Fallback: cached response (60s TTL) (default); DLQ: <service>-dlq; Delivery: at-least-once (default); Replay: yes (default — confirm) | Logs: structured entries (request_id, run_id, status) → Cloud Logging 30d; Metrics: RED via Cloud Monitoring; Tracing: W3C Trace Context via OTel SDK; Health: /healthz every 10s; Alerts: error rate >1% for 5min → PagerDuty primary | Residency: us-central1 only (default; confirm with data-residency policy); Reg scope: SOC 2 (default; confirm with security team); Audit: Cloud Audit Logs 1yr → security archive (default); PII: none in payload (assumed; tag if classification changes) | Upstream: <prev connector(s)>; Downstream: <next connector(s)>; Blast: pipeline halt at this hop (default); Degrade: queue upstream / fail-soft (default); RTO: 1h (default); RPO: 0 (source data persisted) |
| 7 | Ephemeral Cluster (Dataproc) | Staging Tables (BigQuery) | loads raw | Asynchronous; Batch; Push; One-way | BigQuery Storage Write API; gRPC over HTTPS; 443; v1; projects/<p>/datasets/<d>/tables/<t>; AppendRows / Load; Protobuf or Avro | AuthN: Dataproc cluster SA; AuthZ: roles/bigquery.dataEditor (staging) + bigquery.jobUser; Issuer: dataproc-cluster-sa@<project>; TTL: 1h; Secret store: Workload Identity | Format: Parquet (Spark output) → BQ Avro write; Size: avg 50 MB / max 2 GB per partition; Classification: Internal (raw); In-transit: TLS 1.3; At-rest: BQ Google-managed (CMEK for classified); Masking: none at staging; Schema evol: BQ schema relaxation (add nullable cols) | Src: <service>-vpc/<service>-subnet; Tgt: <target> via Private Service Connect; Endpoint: Private (Private Service Connect); Region: us-central1; FW: <service>-egress-<target> firewall rule; DNS: <target>.googleapis.com via private DNS; LB/Mesh: none; Egress: VPC-SC perimeter (default) | Throughput: 1 GB/min per cluster (default); Peak: 5 GB/min with autoscaling; Latency: P50 60s for 100 MB / P95 5min / P99 15min; Timeout: 1800s per load; Concurrency: 4 parallel writers (default); Rate limit: BQ load-jobs 1000/day + slot reservation | Idempotent: yes (partition + dedup on PK); Retry: 3x exp 60/120/300s; CB: per-table; Bulkhead: per-source-prefix; Fallback: GCS holding bucket, replay; DLQ: bq-write-errors table; Delivery: at-least-once (dedup downstream); Replay: yes (re-run from source) | Logs: structured entries (request_id, run_id, status) → Cloud Logging 30d; Metrics: RED via Cloud Monitoring; Tracing: W3C Trace Context via OTel SDK; Health: /healthz every 10s; Alerts: error rate >1% for 5min → PagerDuty primary | Residency: us-central1 only (default; confirm with data-residency policy); Reg scope: SOC 2 (default; confirm with security team); Audit: Cloud Audit Logs 1yr → security archive (default); PII: none in payload (assumed; tag if classification changes) | Upstream: <prev connector(s)>; Downstream: <next connector(s)>; Blast: pipeline halt at this hop (default); Degrade: queue upstream / fail-soft (default); RTO: 1h (default); RPO: 0 (source data persisted) |
| 8 | Data Validation (Dataplex) | Staging Tables (BigQuery) | DQ checks | Asynchronous; Batch; Push; One-way (default — confirm with architect) | HTTPS; REST; 443; v1; <endpoint>; <methods> (default GCP API — confirm endpoint) | AuthN: GCP Service Account (default); AuthZ: <service-specific role>; Issuer: <service>-sa@<project>; TTL: 1h; Secret store: Workload Identity (default) | Format: Parquet (Spark output) → BQ Avro write; Size: avg 50 MB / max 2 GB per partition; Classification: Internal (raw); In-transit: TLS 1.3; At-rest: BQ Google-managed (CMEK for classified); Masking: none at staging; Schema evol: BQ schema relaxation (add nullable cols) | Src: <service>-vpc/<service>-subnet; Tgt: <target> via Private Service Connect; Endpoint: Private (Private Service Connect); Region: us-central1; FW: <service>-egress-<target> firewall rule; DNS: <target>.googleapis.com via private DNS; LB/Mesh: none; Egress: VPC-SC perimeter (default) | Throughput: 100 RPS avg (default); Peak: 500 RPS; Latency: P50 50ms / P95 200ms / P99 500ms (default); Timeout: connect 5s / read 30s / total 60s; Concurrency: 50 (default); Rate limit: service-quota default — confirm | Idempotent: yes (default); Retry: 3x exp + jitter (default); CB: 50% error rate over 60s; Bulkhead: per-tenant 100 conn (default); Fallback: cached response (60s TTL) (default); DLQ: <service>-dlq; Delivery: at-least-once (default); Replay: yes (default — confirm) | Logs: structured entries (request_id, run_id, status) → Cloud Logging 30d; Metrics: RED via Cloud Monitoring; Tracing: W3C Trace Context via OTel SDK; Health: /healthz every 10s; Alerts: error rate >1% for 5min → PagerDuty primary | Residency: us-central1 only (default; confirm with data-residency policy); Reg scope: SOC 2 (default; confirm with security team); Audit: Cloud Audit Logs 1yr → security archive (default); PII: none in payload (assumed; tag if classification changes) | Upstream: <prev connector(s)>; Downstream: <next connector(s)>; Blast: pipeline halt at this hop (default); Degrade: queue upstream / fail-soft (default); RTO: 1h (default); RPO: 0 (source data persisted) |
| 9 | Staging Tables (BigQuery) | Curation Tables (BigQuery) | transforms | Asynchronous; Batch; Push; One-way (default — confirm with architect) | HTTPS; REST; 443; v1; <endpoint>; <methods> (default GCP API — confirm endpoint) | AuthN: GCP Service Account (default); AuthZ: <service-specific role>; Issuer: <service>-sa@<project>; TTL: 1h; Secret store: Workload Identity (default) | Format: business-modelled records (joined / aggregated / normalized); Size: avg 100 MB / max 5 GB per write; Classification: Confidential (downstream consumption); In-transit: TLS 1.3; At-rest: BQ CMEK (curation tier mandated); Masking: PII columns hashed SHA-256 or tokenized; Schema evol: forward-compatible additive only (downstream contract) | Src: <service>-vpc/<service>-subnet; Tgt: <target> via Private Service Connect; Endpoint: Private (Private Service Connect); Region: us-central1; FW: <service>-egress-<target> firewall rule; DNS: <target>.googleapis.com via private DNS; LB/Mesh: none; Egress: VPC-SC perimeter (default) | Throughput: 100 RPS avg (default); Peak: 500 RPS; Latency: P50 50ms / P95 200ms / P99 500ms (default); Timeout: connect 5s / read 30s / total 60s; Concurrency: 50 (default); Rate limit: service-quota default — confirm | Idempotent: yes (default); Retry: 3x exp + jitter (default); CB: 50% error rate over 60s; Bulkhead: per-tenant 100 conn (default); Fallback: cached response (60s TTL) (default); DLQ: <service>-dlq; Delivery: at-least-once (default); Replay: yes (default — confirm) | Logs: structured entries (request_id, run_id, status) → Cloud Logging 30d; Metrics: RED via Cloud Monitoring; Tracing: W3C Trace Context via OTel SDK; Health: /healthz every 10s; Alerts: error rate >1% for 5min → PagerDuty primary | Residency: us-central1 only (default; confirm with data-residency policy); Reg scope: SOC 2 (assumed PCI/HIPAA out-of-scope; confirm with legal per-dataset); Audit: BQ audit log 1yr (who-wrote-what); PII: tokenization applied pre-write; row-level access policies enforced | Upstream: <prev connector(s)>; Downstream: <next connector(s)>; Blast: pipeline halt at this hop (default); Degrade: queue upstream / fail-soft (default); RTO: 1h (default); RPO: 0 (source data persisted) |
| 10 | Ephemeral Cluster (Dataproc) | Cloud Logging | logs | Asynchronous; Streaming; Push; One-way (fire & forget) | gRPC over HTTPS (Cloud Logging API); RPC; 443; v2; projects/<p>/logs/<name>; WriteLogEntries; Protobuf | AuthN: Dataproc SA; AuthZ: roles/logging.logWriter; Issuer: <service>-sa@<project>; TTL: 1h; Secret store: Workload Identity | Format: structured JSON log entries; Size: <10 KB per entry; Classification: Internal (may reference Confidential — never row data); In-transit: TLS 1.3; At-rest: Cloud Logging managed; Masking: row contents stripped at emit per logging-config; Schema evol: JSON free-form additive | Src: <service>; Tgt: managed GCP service; Endpoint: GCP-internal API; Region: us-central1 (multi-region for logs/metrics); FW: GCP-managed; DNS: <service>.googleapis.com; LB/Mesh: none; Egress: n/a | Throughput: 100 entries/sec sustained; Peak: 1000/sec burst; Latency: P50 50ms / P95 200ms / P99 1s buffer flush; Timeout: 30s; Concurrency: per-pod buffer; Rate limit: Cloud Logging API quota | Idempotent: yes (client-assigned entry id); Retry: SDK 3x exp backoff; CB: SDK falls back to local file on persistent failure; Bulkhead: per-pod; Fallback: stderr → GKE log collection; DLQ: n/a; Delivery: at-least-once (best-effort); Replay: no | Logs: meta — Cloud Logging self-monitoring; Metrics: log-entries-written + drop-rate (USE); Tracing: entries carry trace_id label; Health: Cloud Logging status; Alerts: drop-rate >0.1% / 5min → PagerDuty | Residency: us multi-region (default — confirm if residency mandates us-only); Reg scope: SOC 2 (logs/metrics are audit material); Audit: 30d hot + 1yr cold export (default); PII: stripped at emit per logging-config (assumed) | Upstream: any pipeline event; Downstream: SIEM / dashboards / on-call (out-of-scope); Blast: lose visibility (no data loss); Degrade: local agent buffer / log-based metrics; RTO: 5min; RPO: 5min |
| 11 | Orchestrator (Cloud Composer) | Cloud Monitoring | metrics | Asynchronous; Streaming; Push; One-way (fire & forget) | gRPC over HTTPS (Cloud Monitoring API); RPC; 443; v3; projects/<p>/timeSeries; CreateTimeSeries; Protobuf | AuthN: Composer SA; AuthZ: roles/monitoring.metricWriter; Issuer: <service>-sa@<project>; TTL: 1h; Secret store: Workload Identity | Format: time-series points (metric_name, value, timestamp, labels); Size: ~200 B per point; Classification: Internal (aggregates); In-transit: TLS 1.3; At-rest: Cloud Monitoring managed; Masking: aggregates only (no per-row data); Schema evol: stable metric schema with additive labels | Src: <service>; Tgt: managed GCP service; Endpoint: GCP-internal API; Region: us-central1 (multi-region for logs/metrics); FW: GCP-managed; DNS: <service>.googleapis.com; LB/Mesh: none; Egress: n/a | Throughput: 10 points/sec sustained; Peak: 100/sec burst; Latency: P50 100ms / P95 500ms / P99 2s emit; Timeout: 30s; Concurrency: client buffer; Rate limit: 1 pt/min per stream (Cloud Monitoring) | Idempotent: yes (timestamp dedup); Retry: SDK 3x exp; CB: drop on overflow (lossy by design); Bulkhead: per-stream; Fallback: log-based metrics; DLQ: n/a; Delivery: at-least-once; Replay: no (point-in-time) | Logs: meta; Metrics: meta — self-monitoring; Tracing: trace_id as label on points; Health: Cloud Monitoring status; Alerts: pipeline-error-rate >1% / 5min → PagerDuty (PRIMARY alerting surface) | Residency: us multi-region (default — confirm if residency mandates us-only); Reg scope: SOC 2 (logs/metrics are audit material); Audit: 30d hot + 1yr cold export (default); PII: stripped at emit per logging-config (assumed) | Upstream: any pipeline event; Downstream: SIEM / dashboards / on-call (out-of-scope); Blast: lose visibility (no data loss); Degrade: local agent buffer / log-based metrics; RTO: 5min; RPO: 5min |
| 12 | Orchestrator (Cloud Composer) | Audit Table (BigQuery) | updates audit | Synchronous; Request-Response; Push; Two-way | BigQuery Storage Write API; gRPC over HTTPS; 443; v1; projects/<p>/datasets/<d>/tables/<t>; AppendRows / Load; Protobuf or Avro | AuthN: Composer worker SA; AuthZ: roles/bigquery.dataEditor (target table only — least privilege); Issuer: composer-sa@<project>; TTL: 1h; Secret store: Workload Identity | Format: structured audit record (run_id, source_file, counts, status, duration); Size: <1 KB per row; Classification: Internal (audit-grade); In-transit: TLS 1.3; At-rest: BQ CMEK; Masking: none; Schema evol: additive only (audit contract) | Src: <service>-vpc/<service>-subnet; Tgt: <target> via Private Service Connect; Endpoint: Private (Private Service Connect); Region: us-central1; FW: <service>-egress-<target> firewall rule; DNS: <target>.googleapis.com via private DNS; LB/Mesh: none; Egress: VPC-SC perimeter (default) | Throughput: 1 row per pipeline run; Peak: 60/h; Latency: P50 100ms / P95 500ms / P99 2s; Timeout: 30s; Concurrency: 1 per run; Rate limit: BQ Storage Write | Idempotent: yes (PK = run_id); Retry: 5x exp (audit completeness MANDATORY); CB: n/a (always retry); Bulkhead: per-run; Fallback: dual-write to Cloud Logging on BQ unavailable; DLQ: cloud-logging fallback + reconcile job; Delivery: at-least-once; Replay: n/a (single row per run) | Logs: audit-write success/fail → Cloud Logging 1yr; Metrics: audit-row-count + audit-write-latency + audit-write-fail-rate; Tracing: W3C; Health: Composer DAG completion gate; Alerts: audit-write fail → PagerDuty (BLOCKING — page primary on-call) | Residency: us-central1 only (default; confirm with data-residency policy); Reg scope: SOC 2 — audit log REQUIRED for compliance; Audit: this IS the pipeline audit log (3yr retention); PII: none | Upstream: pipeline run lifecycle connectors; Downstream: ops reporting / SOC review (out-of-scope); Blast: lose audit traceability (compliance impact); Degrade: Cloud Logging until BQ recovers, reconcile via batch; RTO: 0 (must succeed before run-complete ack); RPO: 0 |

---
Last review:  `{{date}}` by `{{name}}`
Next review:  `{{date}}` (architecture review board)
